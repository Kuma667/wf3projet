{% extends 'base.html.twig' %}


{% block body %}
	{% include '/_partials/searchBar.html.twig' %}
	
	<h1 class="text-center mt-3 mb-5"><span class="souligne">Liste des annonces</span></h1>
	<div class="container">
		<div class="row">
			<div class="col-md-12">
					{% for annonce in annonces %}
						<div class="row mb-3">
							<div class="col-md-4">
							{# {% for image in annonce.images %}
								<img class="img-fluid" style="max-width:300px" src="{{ asset('uploads/photos/' ~{{ image.name }}">	</br>
							{% endfor %} #}
							{% if annonce.images.first %}
								<img class="img-fluid" style="max-width:300px" src="{{ asset('uploads/photos/' ~ annonce.images.first.name) }}">
							{% endif %}

							</div>
							<div class="col-md-8">
								<h3><a href="{{ path('annonceSingle', {id: annonce.id}) }}"> {{ annonce.titre }} </a> </h3>
								<p class="lead"> {{ annonce.prix }}€ </p>
								<p class="lead"> {{ annonce.ville }} </p>
							</div>
						</div>
					{% endfor %}
			</div>
		</div>
	</div>

{% endblock %}
