{% extends 'base.html.twig' %}


{% block body %}
	
	<h1 class="text-center mt-3 mb-5"><span class="souligne">Liste des articles</span></h1>
	<div class="row">
		<div class="col-md-12">
				{% for annonce in annonces %}
					<div class="row mb-3">
						<div class="col-md-4">
							<img class="img-fluid" style="max-width:300px" src="/uploads/photos/{{ annonce.photo }}">
						</div>
						<div class="col-md-8">
							<h3><a href="{{ path('annonceSingle', {id: annonce.id}) }}"> {{ annonce.titre }} </a> </h3>
							<p class="lead"> {{ annonce.prix }}€ </p>
							<p class="lead"> {{ annonce.ville }} </p>
						</div>
					</div>
				{% endfor %}
		</div>
	</div>

{% endblock %}
